# Web UI（必填）
# 生成方式示例（本机 python 环境）：
#   python -c 'from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())'
APP_MASTER_KEY=
APP_BASIC_AUTH_USER=admin
APP_BASIC_AUTH_PASSWORD=change-me

# Web UI（可选）
APP_PORT=53100
APP_DATA_DIR=/data
APP_DB_URL=sqlite:////data/app.db
APP_POLL_INTERVAL_SECONDS=60

# kp.m-team.cc 预设：直接在 Web UI 用 /accounts/new?preset=kp 创建

